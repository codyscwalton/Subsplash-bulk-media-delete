README

Bulk delete script from a csv file. Created this to get rid of numerous "hidden" media items that would only show in a bulk edit csv export but not the Dashboard.

export bearer_token="insert_token"

python3 bulkdelete.py